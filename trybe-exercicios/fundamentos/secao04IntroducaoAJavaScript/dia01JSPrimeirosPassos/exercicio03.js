const base = 5;
const height = 8;
const area = base * height;

console.log(`base ${base} x height ${height} = ${area}`)

const perimeter = base + height + area;

console.log(`O perímetro do retângulo é ${perimeter}.`)